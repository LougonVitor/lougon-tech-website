/* Âncoras das seções da home, usadas pelo menu e pelos botões de rolagem. */
export const HOME_SECTION_IDS = {
    products: 'produtos',
    differentials: 'diferenciais',
    contact: 'contato',
} as const
